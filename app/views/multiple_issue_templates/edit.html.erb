<h2><%= l(:label_multiple_templates_edit, default: 'Edit Multiple Template') %></h2>
<%= labelled_form_for @template, url: multiple_issue_template_path(@template), method: :put do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= submit_tag l(:button_save) %>
<% end %>